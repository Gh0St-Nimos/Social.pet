-- vai ser necessario criar essa aql no banco de dados, basta copiar o código a seguir.

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    usuario VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL  /* Tem que ter no minimo 255 linhas para a criptografia */
);
